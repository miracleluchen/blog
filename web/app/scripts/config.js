/**
 * Created by luch on 4/21/16.
 */
const DOMAIN = 'http://blog.lemontu.com/';
const HOST = DOMAIN + 'backend/api';
var GLOBAL = {};
const CONFIG = {
  "DOMAIN": DOMAIN + '#',
  "API": {
    'USER': HOST + '/user',
    'POSTS': HOST + '/posts',
    'SEARCH': HOST + '/posts/search/',
    'TAGS': HOST + '/tags',
  }
};

